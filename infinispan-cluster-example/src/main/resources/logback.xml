<configuration>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <!-- encoders are assigned the type
             ch.qos.logback.classic.encoder.PatternLayoutEncoder by default -->
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ResourceDMBean will throw a null pointer exception when logging the debug message in invoke() -->
    <!-- when args is { null }. This happens for example in the cache ___defaultcache command. -->
    <!-- logger name="org.infinispan.jmx.ResourceDMBean" level="INFO"/ -->

    <root level="TRACE">
        <appender-ref ref="STDOUT" />
    </root>

</configuration>
